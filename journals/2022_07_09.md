- commands for update and upgrade [[ubuntu]]
	- we suggest to use
	- sudo apt-get update -> sudo apt update
	- sudo apt list --upgradable
- for install just a specific package type
	- sudo apt-get install package-name